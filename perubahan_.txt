PERUBAHAN — 2026-07-13 (keamanan password)
==========================================

Fitur: Password sekarang di-HASH (scrypt) dan diverifikasi di server. Password
tidak pernah dikirim balik ke browser lagi.

Masalah sebelumnya: GET /api/settings mengembalikan admin_pw + semua password user
(plaintext) ke browser — siapa pun yang buka endpoint bisa baca semua password.
Sekarang GET hanya mengembalikan broadcast + daftar username (tanpa password).

FILE YANG DIUBAH
----------------

1. artifacts/cobol-stream-tool/api/settings.ts
   - Tambah hashPw()/verifyPw() pakai node:crypto scrypt (tanpa dependency baru).
     Format simpan: "scrypt$<salt_hex>$<hash_hex>". Verifikasi pakai timingSafeEqual.
   - GET: hanya { broadcast, users:[username...] } — tidak ada password/hash.
   - POST {action:"login", user, password}: verifikasi di server, balas {ok, user}.
     Auto-upgrade: kalau nilai tersimpan masih plaintext lama, di-rehash saat login
     sukses.
   - POST {action:"save-users", adminPassword, newAdminPassword, users:[{id,password}]}:
     server verifikasi adminPassword dulu (kalau salah -> 401), lalu hash password
     baru. Password kosong utk user lama = pertahankan hash yang ada.
   - PUT hanya untuk broadcast.

2. artifacts/cobol-stream-tool/src/lib/settings-api.ts
   - AppSettings jadi { broadcast, users:string[] } (username saja).
   - Tambah login(), saveUsers(), saveBroadcast(). Hapus putSetting lama.

3. artifacts/cobol-stream-tool/src/components/auth.tsx
   - Login lewat login() (verifikasi server). Fallback admin offline tetap ada.
   - UserManagerDialog: field password user lama kosong ("Leave blank to keep")
     karena hash tidak bisa ditampilkan. Ada field "Admin password (confirm to save)"
     — WAJIB diisi utk otorisasi simpan (karena session cuma simpan username, jadi
     ini satu-satunya proteksi nyata), plus field "New admin password" opsional.
     User baru wajib punya password. Semua input password pakai type=password.

4. artifacts/cobol-stream-tool/src/components/broadcast.tsx
   - putSetting -> saveBroadcast.

MIGRASI DATA
------------
- Password lama (admin_pw, ids, stella) yang masih plaintext di DB sudah di-hash
  sekali jalan. Verifikasi: semua nilai sekarang diawali "scrypt$", tidak ada
  plaintext tersisa.

CATATAN / LOG
-------------
- Dites di localhost (vercel dev) SEBELUM commit/deploy:
  GET tanpa password OK; login benar/salah/user-tak-dikenal OK; save-users password
  admin salah -> 401; save-users benar (tambah user tmp -> ke-hash & bisa login,
  stella tetap bisa login via blank-keep, lalu tmp dihapus utk restore) OK.
- Sisa ceiling: fallback admin offline (ADMIN_FALLBACK_PASSWORD di auth.tsx) masih
  plaintext hardcoded di bundle — memang disengaja utk pintu darurat admin.


PERUBAHAN — 2026-07-13
======================

Fitur: Admin password, daftar user (id + password), dan broadcast sekarang
disimpan di database Postgres (Supabase), bukan lagi di localStorage per-browser.
Data jadi persist lintas sesi dan lintas device — broadcast/user yang dibuat di
satu device muncul di device lain.

Alur: browser -> /api/settings (Vercel serverless function) -> Postgres.
Kredensial DB (DATABASE_URL) hanya ada di server, tidak pernah dikirim ke browser.

FILE YANG DIUBAH / DITAMBAH
---------------------------

1. artifacts/cobol-stream-tool/api/settings.ts (BARU)
   - Vercel serverless function. GET = ambil semua setting; PUT {key,value} =
     upsert satu key (value null = hapus, dipakai untuk clear broadcast).
   - Pakai `pg` (pool max:1, session pooler Supabase). Key valid: admin_pw,
     users, broadcast.

2. artifacts/cobol-stream-tool/api/tsconfig.json (BARU)
   - tsconfig khusus folder api/ supaya Vercel bisa emit function-nya (tsconfig
     app pakai noEmit + allowImportingTsExtensions yang memblok emit).

3. artifacts/cobol-stream-tool/src/lib/settings-api.ts (BARU)
   - Helper client: getSettings(), putSetting(key, value), dan hook useSettings()
     (react-query) untuk cache setting bersama.

4. artifacts/cobol-stream-tool/src/components/auth.tsx
   - Hapus penyimpanan localStorage (admin_pw, users). Login validasi ke server
     (fetch fresh tiap submit). Session user tetap di sessionStorage.
   - Fallback: kalau server tidak bisa dihubungi, admin masih bisa login dengan
     password hardcoded ADMIN_FALLBACK_PASSWORD ("alfredo3gituloh"). Guest tidak
     bisa login offline.
   - UserManagerDialog load/save via API (async), tombol Save punya state "Saving…".

5. artifacts/cobol-stream-tool/src/components/broadcast.tsx
   - Hapus localStorage. BroadcastDialog load/save via API, refresh banner lewat
     invalidate query ["settings"]. Hapus prop onSaved.

6. artifacts/cobol-stream-tool/src/App.tsx
   - QueryClientProvider dinaikkan supaya membungkus login juga (LoginScreen
     pakai useSettings). Banner broadcast dibaca dari useSettings, bukan useState.

7. artifacts/cobol-stream-tool/package.json
   - Tambah dependency: pg; devDependency: @types/pg, @vercel/node.

8. Run DDM.command
   - Ganti `pnpm dev` (vite saja) jadi `vercel dev` supaya function /api ikut
     jalan di lokal (login/users/broadcast bisa dites di localhost).

DATABASE
--------
- Tabel `app_settings (key text primary key, value jsonb, updated_at timestamptz)`
  di Supabase. Seed: admin_pw="alfredo3gituloh", users={"ids":"ids"}.
- Env Vercel: DATABASE_URL diset di production/preview/development. PORT & BASE_PATH
  ditambah ke development/preview supaya build & `vercel dev` jalan.

CATATAN / LOG
-------------
- Deploy pertama gagal: "api/settings.ts: Emit skipped" — diperbaiki dengan
  menambah api/tsconfig.json (noEmit:false).
- Verifikasi: function dites langsung ke Supabase (GET/PUT/clear ok), endpoint live
  https://ddm-streamers.vercel.app/api/settings balas 200, `vercel dev` lokal
  serve UI + /api, typecheck bersih.
- Keamanan: password masih plaintext (login memang gerbang dekoratif), tapi tidak
  lagi world-readable dan tidak lagi di-hardcode di bundle — KECUALI fallback admin
  yang sengaja hardcoded. Kalau password admin diganti lewat dialog Users, update
  juga ADMIN_FALLBACK_PASSWORD di auth.tsx. Upgrade berikutnya: hash (bcrypt).


PERUBAHAN — 2026-07-12
======================

Fitur: FILLER dengan klausa VALUE (mis. FILLER PIC X(1) VALUE 'X') sekarang
diperlakukan seperti field biasa di tabel Generate dan Decompose (editable /
tampil nilainya). FILLER tanpa VALUE tetap read-only berlabel "Filler".

FILE YANG DIUBAH
----------------

1. artifacts/cobol-stream-tool/src/lib/cobol.ts
   - Tambah properti `initialValue: string | null` pada interface ParsedField
     dan RawLine — menampung literal dari klausa VALUE.
   - parseLine(): regex baru untuk parsing VALUE 'X' / VALUE "X" / VALUE 123
     (literal berkutip atau angka; konstanta figuratif SPACES/ZEROS belum
     didukung — ditandai komentar ponytail).
   - parseCopybook(): meneruskan initialValue ke setiap field (null untuk
     baris group).
   - generateStream(): nilai default field diambil dari initialValue bila user
     belum mengetik nilai (values[id] ?? initialValue ?? "").

2. artifacts/cobol-stream-tool/src/components/generate-tab.tsx
   - Input value muncul untuk FILLER yang punya initialValue (sebelumnya semua
     FILLER read-only). Input ter-prefill dengan nilai VALUE dan bisa diedit;
     "Clear Values" mengembalikan ke nilai VALUE.
   - Label italic "Filler" hanya tampil untuk FILLER tanpa VALUE.

3. artifacts/cobol-stream-tool/src/components/decompose-tab.tsx
   - Kolom Value menampilkan isi stream untuk FILLER yang punya VALUE
     (sebelumnya hanya label "Filler").
   - Tombol "To Generate" juga aktif untuk FILLER yang punya VALUE.

4. artifacts/cobol-stream-tool/src/lib/cobol.filler-value.test.ts (BARU)
   - Self-check: jalankan dengan `node src/lib/cobol.filler-value.test.ts`.

CATATAN / LOG
-------------
- Bug ditemukan saat testing: titik penutup baris COBOL ("VALUE 123.") ikut
  tertangkap regex angka sehingga initialValue jadi "123.". Diperbaiki dengan
  regex angka yang lebih ketat: [+-]?\d+(?:\.\d+)?
- "Continue in Generate" tetap melewati FILLER (nama FILLER berulang sehingga
  pemetaan by-name akan saling menimpa); tabel Generate otomatis terisi dari
  VALUE copybook, jadi hasil akhirnya sama.
- Verifikasi: self-check test "ok", `pnpm run typecheck` bersih, dev server
  (http://localhost:5173) memuat perubahan via HMR.

DIFF BEFORE / AFTER
-------------------

--- artifacts/cobol-stream-tool/src/lib/cobol.ts ---

[1] Interface ParsedField — tambah properti:
BEFORE:
  kind: FieldKind; // semantic category ...
}
AFTER:
  kind: FieldKind; // semantic category ...
  initialValue: string | null; // literal from a VALUE clause (e.g. VALUE 'X'); FILLER fields with one behave like normal editable fields
}

[2] Interface RawLine — tambah properti:
BEFORE:
  isGroup: boolean; // true when the line has no PIC clause (group header)
}
AFTER:
  isGroup: boolean; // true when the line has no PIC clause (group header)
  initialValue: string | null;
}

[3] parseLine() — parsing klausa VALUE (kode baru, sebelumnya tidak ada):
AFTER:
  // VALUE 'X' / VALUE "X" / VALUE 123 -- quoted or numeric literals only
  // ponytail: figurative constants (SPACES, ZEROS, ...) not parsed, add when a copybook needs them
  const valueMatch = afterLevel.match(/VALUE\s+(?:'([^']*)'|"([^"]*)"|([+-]?\d+(?:\.\d+)?))/i);
  const initialValue = valueMatch ? valueMatch[1] ?? valueMatch[2] ?? valueMatch[3] : null;

  (return group header: initialValue: null; return field: initialValue,)

[4] parseCopybook() — setiap fields.push() diberi initialValue
    (null untuk baris group/redefines header, parsed.initialValue untuk field).

[5] generateStream() — default nilai dari VALUE:
BEFORE:
    const formatted = formatFieldValue(field, values[field.id] ?? "");
AFTER:
    const formatted = formatFieldValue(field, values[field.id] ?? field.initialValue ?? "");

--- artifacts/cobol-stream-tool/src/components/generate-tab.tsx ---

[1] Kondisi render Input + prefill:
BEFORE:
  {!f.isGroup && !f.isFiller && f.length > 0 && (
    <Input
      className="h-7 text-xs font-mono"
      value={values[f.id] || ""}
AFTER:
  {!f.isGroup && (!f.isFiller || f.initialValue !== null) && f.length > 0 && (
    <Input
      className="h-7 text-xs font-mono"
      value={values[f.id] ?? f.initialValue ?? ""}

[2] Label "Filler":
BEFORE:
  {!f.isGroup && f.isFiller && <span ...>Filler</span>}
AFTER:
  {!f.isGroup && f.isFiller && f.initialValue === null && <span ...>Filler</span>}

--- artifacts/cobol-stream-tool/src/components/decompose-tab.tsx ---

[1] Kolom Value:
BEFORE:
  ) : r.isFiller ? (
    <span className="italic text-muted-foreground">Filler</span>
AFTER:
  ) : r.isFiller && r.initialValue === null ? (
    <span className="italic text-muted-foreground">Filler</span>

[2] Tombol "To Generate":
BEFORE:
  {!r.isGroup && !r.isFiller && r.length > 0 && (
AFTER:
  {!r.isGroup && (!r.isFiller || r.initialValue !== null) && r.length > 0 && (

PERUBAHAN SEBELUMNYA (sesi yang sama, setup lokal)
--------------------------------------------------
- pnpm-workspace.yaml: hapus exclusion binary darwin-arm64 (esbuild, rollup,
  lightningcss, @tailwindcss/oxide) agar bisa jalan di macOS; izinkan build
  script esbuild (allowBuilds: esbuild: true).

  BEFORE:
    "esbuild>@esbuild/darwin-arm64": "-"
    "lightningcss>lightningcss-darwin-arm64": "-"
    "@tailwindcss/oxide>@tailwindcss/oxide-darwin-arm64": "-"
    "rollup>@rollup/rollup-darwin-arm64": "-"
  AFTER:
    (empat baris di atas dihapus)
    allowBuilds:
      esbuild: true


======================================================================
PERUBAHAN — 2026-07-12 (update 2)
======================================================================

Fitur:
1. Tombol Refresh di tab Generate dan Decompose.
   - Generate: mengembalikan semua field yang punya klausa VALUE ke literal
     VALUE terbaru di copybook (menghapus ketikan user pada field tsb).
   - Decompose: memaksa parse ulang copybook + dekomposisi ulang stream.
2. Dukungan klausa VALUE COBOL yang lebih lengkap (hasil riset semantik COBOL):
   - VALUE [IS] 'lit' / "lit" / angka         (sudah ada sebelumnya)
   - VALUE ZEROS / ZEROES / ZERO              -> field terisi '0' penuh
   - VALUE SPACES / SPACE                     -> field terisi spasi penuh
   - VALUE QUOTE / QUOTES                     -> karakter '"'
   - VALUE ALL "*" / VALUE IS ALL 'AB'        -> literal diulang sampai
                                                 memenuhi panjang field
   - HIGH-VALUES / LOW-VALUES TIDAK didukung (byte 0xFF/0x00, non-printable,
     tidak relevan untuk stream teks) — ditandai komentar ponytail.

FILE YANG DIUBAH
----------------

1. artifacts/cobol-stream-tool/src/lib/cobol.ts
   - Fungsi baru parseValueClause(afterLevel, length) menggantikan regex
     inline di parseLine; parsing VALUE kini butuh panjang field (untuk
     ekspansi ALL/ZEROS/SPACES) sehingga dipindah ke setelah parsing PIC.

BEFORE (di parseLine, sebelum blok if (!picMatch)):
  // VALUE 'X' / VALUE "X" / VALUE 123 -- quoted or numeric literals only
  // ponytail: figurative constants (SPACES, ZEROS, ...) not parsed, add when a copybook needs them
  const valueMatch = afterLevel.match(/VALUE\s+(?:'([^']*)'|"([^"]*)"|([+-]?\d+(?:\.\d+)?))/i);
  const initialValue = valueMatch ? valueMatch[1] ?? valueMatch[2] ?? valueMatch[3] : null;

AFTER (fungsi baru di atas parseLine):
  function parseValueClause(afterLevel: string, length: number): string | null {
    const m = afterLevel.match(
      /VALUE\s+(?:IS\s+)?(ALL\s+)?(?:'([^']*)'|"([^"]*)"|([+-]?\d+(?:\.\d+)?)|(ZEROE?S?|SPACES?|QUOTES?))/i,
    );
    if (!m) return null;
    const [, all, singleQuoted, doubleQuoted, numeric, figurative] = m;
    let literal = singleQuoted ?? doubleQuoted ?? numeric ?? null;
    if (figurative) {
      const fig = figurative.toUpperCase();
      if (fig.startsWith("ZERO")) literal = "0".repeat(Math.max(length, 1));
      else if (fig.startsWith("SPACE")) literal = " ".repeat(Math.max(length, 1));
      else literal = '"';
    }
    if (literal !== null && all && literal.length > 0 && length > 0) {
      literal = literal.repeat(Math.ceil(length / literal.length)).slice(0, length);
    }
    return literal;
  }

  dan di parseLine setelah parsePicture:
  const initialValue = parseValueClause(afterLevel, length);

2. artifacts/cobol-stream-tool/src/components/generate-tab.tsx
   - Fungsi baru refreshDefaults(): hapus override user pada field ber-VALUE.
   - Tombol Refresh (ikon RefreshCw) di header "Field Values"; tombol
     Clear Values ganti ikon ke Trash2.

BEFORE:
  const clearValues = () => setValues({});
  ...
  <h3 ...>Field Values</h3>
  <Button variant="ghost" size="sm" onClick={clearValues} ...>
    <RefreshCw .../> Clear Values
  </Button>

AFTER:
  const clearValues = () => setValues({});
  const refreshDefaults = () => {
    setValues(prev => {
      const next = { ...prev };
      for (const f of fields) {
        if (f.initialValue !== null) delete next[f.id];
      }
      return next;
    });
    toast({ title: "Refreshed", description: "VALUE fields now follow the copybook." });
  };
  ...
  <h3 ...>Field Values</h3>
  <div className="flex items-center gap-1">
    <Button ... onClick={refreshDefaults} title="Re-apply the copybook's VALUE literals to their fields">
      <RefreshCw .../> Refresh
    </Button>
    <Button ... onClick={clearValues}><Trash2 .../> Clear Values</Button>
  </div>

3. artifacts/cobol-stream-tool/src/components/decompose-tab.tsx
   - State baru refreshNonce; useMemo parseCopybook kini juga bergantung pada
     nonce sehingga tombol Refresh memaksa parse ulang.
   - Tombol Refresh di header "Decomposed Fields".

BEFORE:
  import { useMemo } from "react";
  ...
  const fields = useMemo(() => { ... }, [copybookSource]);

AFTER:
  import { useMemo, useState } from "react";
  ...
  const [refreshNonce, setRefreshNonce] = useState(0);
  const fields = useMemo(() => { ... }, [copybookSource, refreshNonce]);
  ...
  <Button ... onClick={() => { setRefreshNonce(n => n + 1); toast(...); }}>
    <RefreshCw .../> Refresh
  </Button>

4. artifacts/cobol-stream-tool/src/lib/cobol.filler-value.test.ts
   - Test tambahan: ZEROS, ALL "*", SPACES, VALUE IS ALL 'AB', ZERO pada
     field numerik, dan hasil generateStream gabungan.

CATATAN / LOG
-------------
- Perilaku "otomatis refresh": tabel memang selalu reaktif terhadap perubahan
  copybook; tombol Refresh menangani kasus di mana user sudah mengetik nilai
  di field ber-VALUE (ketikan tsb menimpa VALUE sampai di-refresh).
- Verifikasi: self-check test "ok", pnpm run typecheck bersih.

=== 2026-07-12 ===
File: Run DDM.command (BARU)
BEFORE: (tidak ada)
AFTER: script zsh double-click — start Vite dev server cobol-stream-tool (PORT=5173, BASE_PATH=/) jika belum jalan, lalu buka http://localhost:5173 di browser.

=== 2026-07-12 ===
File: artifacts/cobol-stream-tool/src/components/auth.tsx (BARU)
BEFORE: (tidak ada)
AFTER: login screen + dialog kelola guest user. Admin hardcoded: alfredo/alfredo3gituloh. Guest disimpan di localStorage key "ddm_users", seed awal {ids: "ids"}. Admin bisa tambah/edit/hapus guest (ID & password); session login di sessionStorage key "ddm_user".

File: artifacts/cobol-stream-tool/src/App.tsx
BEFORE: App langsung render tabs Generate/Decompose tanpa login.
AFTER: state `user` dari sessionStorage; jika belum login render <LoginScreen>. Header tambah: tombol Users (khusus alfredo), nama user, tombol Logout.

=== 2026-07-12 (redesign via taste skill) ===
File: artifacts/cobol-stream-tool/src/index.css
BEFORE: font Inter + palet default shadcn (abu-abu dingin slate, primary biru jenuh 221 83% 53%).
AFTER: font Geist (+ JetBrains Mono tetap), palet warm monochrome (background warm bone, foreground warm near-black, primary near-black/near-white, destructive merah desaturasi), border warm gray, tabular-nums untuk angka, headline tracking-tight + text-wrap balance, feedback tekan tombol scale(0.98). Dark mode ikut jadi warm charcoal.

File: artifacts/cobol-stream-tool/src/components/auth.tsx
BEFORE: header card login = ikon + judul satu baris.
AFTER: ikon di atas, judul text-lg tracking-tight, subjudul "Sign in to continue" (CardDescription).

=== 2026-07-12 (admin ganti password) ===
File: artifacts/cobol-stream-tool/src/components/auth.tsx
BEFORE: password admin alfredo hardcoded, tidak bisa diganti.
AFTER: dialog Users tambah field "Admin password (alfredo)". Password baru disimpan di localStorage key "ddm_admin_pw"; login cek localStorage dulu, fallback ke default alfredo3gituloh. Hapus localStorage = kembali ke password default.

=== 2026-07-12 (login pakai gambar COBOL) ===
File: COBOLpic.png -> artifacts/cobol-stream-tool/public/cobol.png (disalin)
File: artifacts/cobol-stream-tool/src/components/auth.tsx
BEFORE: login = card di tengah layar polos.
AFTER: split-screen — kiri gambar CRT COBOL (object-cover, tampil di layar lg ke atas), kanan form login (card tanpa border/shadow). Mobile: gambar disembunyikan, form tetap di tengah.

=== 2026-07-12 (fitur Delimiter Export) ===
File: artifacts/cobol-stream-tool/src/components/decompose-tab.tsx
BEFORE: Stream Data dianggap 1 record; tidak ada export CSV.
AFTER: tiap baris non-kosong di Stream Data = 1 record fixed-width. Tabel menampilkan record pertama (ada catatan bila multi-record). Tombol baru "Delimiter Export" — decompose semua baris pakai copybook, download CSV (delimiter koma, header nama field, escaping standar CSV, FILLER murni dilewati). Badge stream jadi "N records × M bytes" bila multi-baris; cek panjang pakai baris pertama.

=== 2026-07-12 (Delimiter Export jadi tab sendiri) ===
File: artifacts/cobol-stream-tool/src/components/decompose-tab.tsx
BEFORE: berisi fitur multi-record + tombol Delimiter Export.
AFTER: dikembalikan ke perilaku asli (1 record, tanpa export).

File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx (BARU)
BEFORE: (tidak ada)
AFTER: tab baru — input copybook + record lines (paste/upload, 1 baris = 1 record), preview CSV 5 baris pertama, tombol Download CSV (koma, header nama field, escaping CSV, FILLER murni dilewati). Semua diproses di browser.

File: artifacts/cobol-stream-tool/src/App.tsx
BEFORE: 2 tab (Generate, Decompose).
AFTER: 3 tab — tambah "Delimiter Export" dengan state sendiri (delimCopybook, delimData).

=== 2026-07-12 (custom delimiter) ===
File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: delimiter tetap koma.
AFTER: input "Delimiter" (max 3 karakter, default koma) di samping tombol download; preview & CSV pakai delimiter pilihan; escaping quote menyesuaikan delimiter; input kosong = fallback koma.

=== 2026-07-12 (proteksi delimiter vs simbol di data) ===
File: artifacts/cobol-stream-tool/src/lib/delimit.ts (BARU)
BEFORE: logika escape/join di dalam komponen.
AFTER: helper murni escapeValue/isDataField/delimitLines — nilai yang mengandung delimiter, kutip, atau newline dibungkus kutip ganda (RFC 4180, kutip di-escape jadi ""), kolom tidak pernah bergeser.

File: artifacts/cobol-stream-tool/src/lib/delimit.test.ts (BARU)
AFTER: node src/lib/delimit.test.ts — assert koma/semicolon/pipe/kutip. Lulus.

File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: escape lokal, tanpa peringatan.
AFTER: pakai lib/delimit; tambah peringatan amber "N records contain <delim> inside their data" agar user sadar nilai dikutip / bisa pilih delimiter lain.

=== 2026-07-12 (Delimiter Export: upload-only, tanpa preview isi) ===
File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: copybook & record lines pakai FileTextarea (isi file tampil penuh, bisa diketik).
AFTER: diganti komponen FileUpload ringkas — tombol "Upload file" + status centang hijau "namafile uploaded — N fields/records...". Isi file tidak ditampilkan. Preview CSV 5 baris & warning mismatch/collision tetap ada.

=== 2026-07-12 (indikator delimiter ada/tidak di data) ===
File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: peringatan collision hanya muncul bila delimiter ditemukan di data.
AFTER: indikator selalu tampil saat mengetik delimiter — amber '"x" already exists in the data (N records)' atau hijau '"x" doesn't appear anywhere in the data — safe delimiter'.

=== 2026-07-13 (crosscheck record pertama/terakhir + hitungan baris) ===
File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: hanya preview CSV 5 baris pertama.
AFTER: tambah (1) baris hitungan "Input: N records → CSV: N+1 rows (N data + 1 header)" dengan centang hijau; (2) tabel crosscheck per variabel copybook — kolom Field, Type, Record 1, Record N (terakhir) — untuk memastikan pemisahan data sesuai harapan. Preview CSV tetap ada.

=== 2026-07-13 (urutan section Delimiter Export) ===
File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: tabel crosscheck di atas indikator delimiter & preview CSV.
AFTER: urutan jadi — hitungan baris, indikator delimiter, preview CSV, lalu tabel "Per-variable crosscheck" (diberi judul) di bawah.

=== 2026-07-13 (pilih field yang diekspor ke CSV) ===
File: artifacts/cobol-stream-tool/src/lib/delimit.ts
BEFORE: delimitLines(fields, lines, delim) — semua field data selalu jadi kolom CSV.
AFTER: delimitLines(fields, lines, delim, excluded?) — parameter opsional Set berisi id field yang di-exclude; field tersebut dilewati di header maupun baris data.

File: artifacts/cobol-stream-tool/src/components/delimiter-export-tab.tsx
BEFORE: tabel crosscheck hanya menampilkan Field/Type/Record 1/Record terakhir; download CSV selalu semua kolom.
AFTER: tambah kolom checkbox di tabel crosscheck — uncheck field = kolomnya tidak ikut di preview & CSV yang di-download (misal 5 field, uncheck no. 2 & 5 → CSV berisi field 1,3,4). Checkbox di header = check/uncheck semua. Baris yang di-exclude tampil redup (opacity-50). Baris hitungan tambah "X of Y fields". Semua unchecked → tombol Download disabled + peringatan amber. Pilihan direset saat upload copybook baru / Clear Tab.

File: artifacts/cobol-stream-tool/src/lib/delimit.test.ts
BEFORE: belum ada test exclude.
AFTER: tambah assert — exclude NAME → header "CITY", row "PARIS".
Log: node src/lib/delimit.test.ts lulus semua assert; npx tsc --noEmit bersih.

=== 2026-07-13 (broadcast message admin) ===
File: artifacts/cobol-stream-tool/src/components/broadcast.tsx (BARU)
BEFORE: (tidak ada)
AFTER: fitur broadcast — admin isi Message + Link URL (opsional) + Link text (opsional) lewat dialog "Broadcast" (ikon megaphone). Disimpan di localStorage key "ddm_broadcast". BroadcastBanner menampilkan pesan + hyperlink (buka tab baru); URL tanpa http:// otomatis diprefix https://. Tombol "Clear broadcast" menghapus pengumuman. Kosongkan semua field + Save = hapus juga.

File: artifacts/cobol-stream-tool/src/App.tsx
BEFORE: header admin hanya tombol Users; main langsung tabs.
AFTER: tambah tombol Broadcast (khusus alfredo) di header; banner pengumuman tampil di atas tabs untuk semua user yang login.

File: artifacts/cobol-stream-tool/src/components/auth.tsx
BEFORE: login screen hanya form.
AFTER: banner pengumuman juga tampil di bawah form login (halaman depan).
Log: npx tsc --noEmit bersih; PORT=5173 npx vite build sukses. Catatan: broadcast tersimpan per-browser (localStorage), sama seperti daftar user — perlu backend bila harus tampil lintas perangkat.

=== 2026-07-13 (estetika: foto CRT COBOL via taste skill) ===
File: images_data.jpeg -> artifacts/cobol-stream-tool/public/cobol-code.jpeg (disalin)
File: artifacts/cobol-stream-tool/src/components/auth.tsx
BEFORE: panel kiri login pakai cobol.png polos.
AFTER: panel kiri pakai foto close-up layar CRT kode COBOL (cobol-code.jpeg) + overlay scanline CRT (repeating-linear-gradient) + vignette radial gelap di tepi. Efek layar tabung lebih terasa.

File: artifacts/cobol-stream-tool/src/components/empty-state.tsx (BARU)
BEFORE: (tidak ada)
AFTER: komponen EmptyState — kartu dengan foto CRT sebagai latar redup (overlay hitam 65% + scanline), teks mono putih di tengah.

File: generate-tab.tsx, decompose-tab.tsx, delimiter-export-tab.tsx
BEFORE: pesan kosong = kotak abu-abu polos bg-muted/50.
AFTER: ketiganya pakai <EmptyState> bertekstur foto CRT. Warna hijau fosfor hanya muncul lewat foto, palet warm monochrome aplikasi tidak berubah.
Log: tsc --noEmit bersih; vite build sukses.
